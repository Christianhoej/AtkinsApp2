import React from 'react';
import { Button } from 'react-native';
/*
const connection = props => {
    return (
    <Button title="Test Connection" onPress={props.onTryConnect}> )
};

export default connection;
*/